import Link from 'next/link';
import { CATALOG_STATS } from '@/data/catalog';

export default function AboutPage() {
  return <main className="max-w-5xl mx-auto px-4 py-12"><div className="mb-10"><p className="text-sm font-semibold text-blue-600 uppercase tracking-wide">NTParts</p><h1 className="text-4xl font-bold text-gray-900 mt-2">Catalogue & data quality</h1><p className="text-gray-600 mt-4 max-w-3xl leading-7">A searchable truck-parts reference catalogue designed to separate discovery data from confirmed fitment. The application never treats a manufacturer homepage as proof that an individual part fits a particular vehicle.</p></div><div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10"><Stat label="Manufacturers" value={CATALOG_STATS.manufacturers} /><Stat label="Models" value={CATALOG_STATS.models} /><Stat label="Part records" value={CATALOG_STATS.parts} /><Stat label="OEM records" value={CATALOG_STATS.verifiedOEMReferences} /></div><div className="space-y-6"><section className="bg-white border rounded-xl p-6"><h2 className="text-xl font-bold text-gray-900">Verification levels</h2><ul className="mt-4 space-y-3 text-gray-600 text-sm leading-6 list-disc pl-5"><li><strong>Verified:</strong> reserved for a record whose source explicitly proves the reference/application relationship.</li><li><strong>Source listed:</strong> the reference is associated with an authoritative manufacturer/OEM source, but exact application still requires catalogue/VIN confirmation.</li><li><strong>Needs verification:</strong> do not use the record as an ordering decision without independent confirmation.</li></ul></section><section className="bg-white border rounded-xl p-6"><h2 className="text-xl font-bold text-gray-900">How to use the catalogue</h2><ol className="mt-4 space-y-3 text-gray-600 text-sm leading-6 list-decimal pl-5"><li>Search by OEM/reference, part name or truck model.</li><li>Open the part record and inspect its manufacturer source.</li><li>Confirm model, generation, engine, axle/chassis and VIN where required.</li><li>Only then use an aftermarket cross-reference or place an order.</li></ol></section></div><Link href="/parts" className="inline-block mt-8 text-blue-600 font-semibold hover:underline">Browse parts →</Link></main>;
}
function Stat({ label, value }: { label: string; value: number }) { return <div className="bg-gray-50 border rounded-xl p-5"><div className="text-2xl font-bold text-gray-900">{value}</div><div className="text-sm text-gray-500 mt-1">{label}</div></div>; }
